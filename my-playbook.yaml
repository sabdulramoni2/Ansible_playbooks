---
- name: Configure nginx web server
  hosts: webservers
  tasks:
  - name: Install nginx server
    apt: 
      name: nginx=1.26*
      state: present
  - name: Start nginx server
    service:
      name: nginx
      state: started